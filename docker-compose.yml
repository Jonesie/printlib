# Standalone local run/dev. For production, run this same Dockerfile as a
# service in your own reverse-proxy stack (see README.md) instead of
# publishing port 8000 directly.
services:
  printlib:
    build: .
    image: printlib:local
    volumes:
      - ./data:/data
    ports:
      - "8000:8000"
